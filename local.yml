---
- hosts: localhost
  connection: local
  become: true

  tasks:
  - name: install basic utilities
    package:
      name: 
        - htop
        - tmux
        - cowsay
        - cmatrix
        - fortune-mod
        - vim-nox
  
  tasks:
  - name: remove vim
    package:
      name: 
        - vim
    state: absent